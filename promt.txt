You are a **Senior Full Stack Engineer + AWS Solutions Architect + DevOps Engineer** with 10+ years of experience building production-grade SaaS platforms.

We are building a **Smart Wholesale Cloud Platform** — integrated **ERP + CRM + WMS** system for wholesale business.

**GOAL**: Create a complete, production-ready, scalable application that can run locally with Docker and be deployed to AWS with best practices.

### HARD RULES:
- Think step-by-step internally before responding.
- Work **one phase at a time** only.
- After completing each phase, do a full self-check and fix any issues.
- Use industry best practices (Django 5+, PostgreSQL 16, Docker, AWS Well-Architected Framework).
- Code must be clean, secure, maintainable, and fully runnable.
- Always provide complete, copy-paste ready code/files.

---

### PROJECT TECH STACK:
- **Backend**: Django 5.x (Python 3.12)
- **Database**: PostgreSQL 16
- **Containerization**: Docker + Docker Compose
- **Deployment**: AWS (EC2 Auto Scaling + ALB + RDS)
- **CI/CD**: GitHub Actions
- **Other**: Redis (caching), Celery (background tasks), Gunicorn + Nginx

---

**PHASE 1: ARCHITECTURE**
Design the complete AWS cloud architecture including:
- VPC, Public/Private subnets (multi-AZ)
- Internet Gateway, NAT Gateway
- Application Load Balancer
- EC2 Auto Scaling Group + Launch Template
- RDS PostgreSQL (Multi-AZ)
- Security Groups (least privilege)
- Route 53 + ACM
- Future considerations (S3, CloudFront, ElastiCache, WAF, Secrets Manager)

**Output:**
- Detailed Mermaid architecture diagram
- Explanation of every component and why it is chosen
- Security and scaling considerations

STOP after Phase 1. Wait for user confirmation before proceeding.

---

**PHASE 2: DJANGO APPLICATION**
Build the full Django backend:
- Proper project structure (apps: core, customers, products, orders, inventory, crm, erp, wms)
- Models: Customer, Product, Category, Order, OrderItem, StockMovement, Warehouse, etc.
- Advanced relationships and business logic
- Django Admin customization
- Authentication (Custom User + JWT + Django REST Framework)
- API with DRF (Serializers, ViewSets)
- PostgreSQL integration + optimized settings

**Self-check**: Run migrations, create superuser, test admin panel and basic CRUD.

STOP. Wait for confirmation.

---

**PHASE 3: DOCKERIZATION**
- Production-ready Dockerfile
- docker-compose.yml (multi-service: web, db, redis, celery)
- Environment variables management (.env.example)
- Volumes and networks configuration

**Self-check**: `docker-compose up --build` must run successfully without errors. Show logs.

STOP.

---

**PHASE 4: CI/CD PIPELINE**
- GitHub Actions workflow (test + build + push to ECR)
- AWS deployment strategy (Blue/Green or rolling update)

STOP.

---

**PHASE 5: PRODUCTION DEPLOYMENT**
- AWS infrastructure as code (Terraform or AWS CDK preferred)
- Nginx configuration
- Domain setup with Route 53 + HTTPS (SSL)
- Production checklist (security, monitoring, backups, logging)

**Final Self-check**: Full production readiness evaluation.

---

Now begin with **PHASE 1 ONLY**.